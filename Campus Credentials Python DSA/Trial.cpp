
class Student:
{
    int roll;
    string name;
    float per;
};

int main()
{ 
    Student s1, s2, s3; # Create 3 objects of class Student
    Student *ptr; # Store the address of object in pointer
    ptr = &s1; # Store the address of object s1 in pointer ptr
}

